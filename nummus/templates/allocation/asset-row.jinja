<a class="btn-text block relative text-on-surface px-6"
   href="{{ url_for('assets.page', uri=asset.uri) }}"
   hx-get="{{ url_for('assets.page', uri=asset.uri) }}"
   hx-disabled-elt="this">
    <div class="flex justify-between gap-2">
        <b class="truncate">{{ asset.name }}</b>
        <span>{{ asset.value | money }}</span>
    </div>
    <div class="flex justify-between gap-2 text-sm text-on-surface-variant">
        <span>
            {% if asset.ticker %}@{{ asset.ticker }},{% endif %}
            {% if include_weight %}{{ asset.weight | percent }} of asset{% endif %}
        </span>
        <span>{{ asset.qty | qty }} @ {{ asset.price | money }}</span>
    </div>
    <icon class="absolute right-0 top-1/2 -translate-y-1/2">chevron_forward</icon>
</a>
