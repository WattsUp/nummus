<nav id="nav-bar">
    {% for label, (icon, endpoint, link) in nav_items[0][1].items() %}
        <button>
            <icon>{{ icon }}</icon>
            <div>{{ label }}</div>
        </button>
    {% endfor %}
    <button title="Add Transaction" id="nav-fab">
        <icon>note_stack_add</icon>
    </button>
</nav>
