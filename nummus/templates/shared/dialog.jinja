<div id="dialog"
     class="dialog z-48"
     hx-swap-oob="{{ oob }}"
     hx-target="#dialog"
     hx-on::oob-before-swap="dialog.reset()"
     hx-on::after-settle="dialog.updateSave()">{{ content | safe }}</div>
