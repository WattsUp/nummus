<a class="bg-white p-2 cursor-pointer group flex flex-col min-h-[20rem] h-[20rem] col-span-2"
   href="{{ url_for("net_worth.page") }}"
   hx-get="{{ url_for("net_worth.page") }}"
   hx-target="#main"
   hx-swap="innerHTML"
   hx-push-url="true">
    <div class="w-full flex space-between">
        <h1 class="text-xl align-left w-full font-serif text-green-600 group-hover:underline group-hover:after:content-['»']">
            Net Worth
        </h1>
        <h1 class="text-lg">{{ chart["current"] | money }}</h1>
    </div>
    <div class="w-full grow">
        <canvas id="net-worth-chart-canvas"></canvas>
    </div>
    <div id="net-worth-chart-data">
        <script>netWorthChart.updateDashboard(JSON.parse('{{ chart["data"] | tojson }}'))</script>
    </div>
</a>
