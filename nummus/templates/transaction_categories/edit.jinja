{% with headline=("Edit" if category["uri"] else "New") +" category" %}
    {% include "/shared/dialog-headline.jinja" %}
{% endwith %}
<form class="grid gap-2">
    <label class="input-outlined input-bg-grey-50 dark:input-bg-grey-950">
        {# TODO (WattsUp): Move to next field on enter #}
        <input name="name"
               required
               enterkeyhint="next"
               placeholder=""
               value="{{ category["name"] or "" }}"
               autocomplete="off"
               autofocus>
        <div>
            <div>Name</div>
        </div>
    </label>
    <label class="input-outlined input-bg-grey-50 dark:input-bg-grey-950 col-start-2">
        <select name="group" required autocomplete="off">
            <option value=""
                    disabled
                    hidden
                    {% if not category["group"] %}selected{% endif %}></option>
            {% for group in category["group_type"] %}
                <option value="{{ group.name }}"
                        {% if group == category["group"] %}selected{% endif %}>{{ group.pretty }}</option>
            {% endfor %}
        </select>
        <div>
            <div>Group</div>
        </div>
    </label>
    <label class="toggle-green col-span-2">
        <input type="checkbox"
               name="is-pnl"
               autocomplete="off"
               {% if category["is_profit_loss"] %}checked{% endif %}>
        <div>
            <span>Include in profit &amp; loss</span>
            <span>Such as dividends &amp; fees</span>
        </div>
        {# djlint: off #}
        <switch></switch>
        {# djlint: on #}
    </label>
    <label class="toggle-green col-span-2">
        <input type="checkbox"
               name="is-essential"
               autocomplete="off"
               {% if category["is_essential"] %}checked{% endif %}
               disabled>
        <div>
            <span>Essential spending</span>
            <span>For emergency fund estimation</span>
        </div>
        {# djlint: off #}
        <switch></switch>
        {# djlint: on #}
    </label>
</form>
{# TODO (WattsUp): Add delete with confirmation dialog
<button class="btn-text-green">
    <icon>delete</icon>Delete
</button>
#}
